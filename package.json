{
  "name": "attorni-landing-page-1",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy",
    "pages:deploy": "astro build && wrangler pages deploy",
    "pages:create": "wrangler pages project create attorni-landing --production-branch=main"
  },
  "dependencies": {
    "astro": "^6.1.3"
  },
  "devDependencies": {
    "wrangler": "^4.80.0"
  }
}